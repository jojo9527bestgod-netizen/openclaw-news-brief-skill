# 2026-03-02

## 会话压缩摘要（用于新会话快速接续）
- 已完成并确认：`memory-lancedb-pro` 插件安装、接入、启用（memory slot 已切换到 `memory-lancedb-pro`）。
- 关键排障结论：仅在终端 `export JINA_API_KEY` 不够；Gateway 作为 launchd 服务进程常读不到。应写入 `~/.openclaw/.env` 并重启 Gateway。
- 用户反馈：曾因缺失 key 导致崩溃，排查耗时较长；后续凡涉及插件密钥，先做“服务进程可见性”检查。
- 当前语境 token 偏高（约 43k/272k），用户要求执行“会话压缩+规则固化”方案，并准备切换到新会话继续。

## 长期规则（确认继续生效）
- 回复语言：中文。
- 新闻简报：北京时间“前一日 00:00-23:59”；总计 15 条；国内/国际各至少 5 条；带编号；每条仅三段：
  - 【标题】
  - 【发生了什么】
  - 【影响/看点】
  语气克制事实导向，结尾固定免责声明。
- 记账：默认账户“现金人民币”；分类规范映射（如“厨房用品”→“家居用品”）。

## 运行状态备注
- `memory-lancedb-pro` 已启用，但通过插件工具写入时出现 embedding `422`，需后续继续校验 Jina embedding 接口参数兼容性。
- 在兼容性彻底确认前，关键记忆继续双写到 `MEMORY.md` + `memory/YYYY-MM-DD.md`，确保可追溯与可恢复。
