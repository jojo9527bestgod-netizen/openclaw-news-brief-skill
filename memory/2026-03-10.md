- 用户要求后续补装 3 个因 ClawHub 限流未完成的 skills：`find-skills`、`skill-vetter`、`summarize`。建议明天换时间段、一次只装一个。
- 用户追问确认：`agent-browser` 是否可用于后续 ezBookkeeping 网页操作；结论是可以显著提升浏览器自动化能力，但仍需按页面结构与登录态实际验证，不等于所有流程百分百无脑成功。
- 用户追问确认：`summarize` 是否可与现有视频总结链路配合；结论是可以，适合在音频转写完成后对 transcript / 字幕文本做二次摘要与多格式输出。
